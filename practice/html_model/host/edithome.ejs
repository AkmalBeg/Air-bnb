<%-include ('../partial/doc') %>
</head>
<body>
    <%-include ('../partial/header') %> 

<main class="min-h-screen bg-gray-100 flex items-center justify-center">
  <div class="w-full max-w-lg bg-white p-8 rounded-2xl shadow-lg">
    
    <h1 class="text-3xl font-bold mb-6 text-center text-gray-800">
      Edit your home
    </h1>

   
     <form action="/host/edithome/<%= home.id %>" method="POST" class="space-y-4">
<input type="text" name="id" value="<%= home.id %>" hidden>
<div>
  <label for="photourl" class="block text-gray-700 font-medium mb-1">
    Photo URL
  </label> 
  <input
          type="text"
          name="photoUrl"
          value="<%= home.photourl %>"
          placeholder=""
          class="w-full px-4 py-2 mb-4 border rounded-md focus:outline-none focus:ring-2 focus:ring-red-500"/>
        <input  />
</div>

      <div>
        <label for="name" class="block text-gray-700 font-medium mb-1">
          Home Name
        </label>
        <input 
          type="text" 
          id="name" 
          name="name" 
          value="<%=  home.name %>"
          required
          class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-red-500"
        >
      </div>

      <div>
        <label for="description" class="block text-gray-700 font-medium mb-1">
          Description
        </label>
        <input 
          type="text" 
          id="description" 
          name="description" 
          value="<%= home.description %>"
          required
          class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-red-500"
        >
      </div>

      <div>
        <label for="price" class="block text-gray-700 font-medium mb-1">
          Price per night
        </label>
        <input 
          type="number" 
          id="price" 
          name="price" 
          value="<%= home.price %>"
          required
          class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-red-500"
        >
      </div>

      <button 
        type="submit"
        class="w-full bg-red-500 text-white py-2 rounded-lg hover:bg-red-600 transition duration-300"
      >
        edit Home
      </button>

    </form>

  </div>
</main>
</body>
</html>